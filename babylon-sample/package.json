{   
    "name": "babylonAutocomplete",
    "version": "0.0.0",
    "scripts": {
        "compile": "cp ./src/index.html ./out/ && coffee --watch --output ./out ./src"
    },
    "dependencies": {},
    "devDependencies": {
        "coffeescript": "^2.3.2"
    }
}